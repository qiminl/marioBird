
#pragma strict

public class Character{
	
	
	public function Character(){}
/*
	public function edgeDetection(){
		if(rigidbody.position.x < -13){ 
				rigidbody.position.x = -13;
		}
		
		else if(rigidbody.position.x > 13){
			rigidbody.position.x = 13;
		}
		
		if(rigidbody.position.y < -8.5){
		 	rigidbody.position.y = -8.5;
		}
		else if(rigidbody.position.y > 8.5){
			rigidbody.position.y = 8.5;
		}
	}
	*/
}